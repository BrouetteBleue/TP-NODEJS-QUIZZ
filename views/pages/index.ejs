<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <%- include('partials/head') %>
    
</head>
<body>
    <%- include('partials/header') %>
    <h1>Themes incroyable</h1>

   
    <div class="container">
        Sélectionnez un thème :
            <% themes.forEach( (t) => { %>
                <button class="btn btn-warning"value="<%= t %>" onclick="startQuizz(this.value)"><%= t %></button>
            <% }) %>
    </div>
</body>
</html>